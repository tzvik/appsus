const { Route, Switch, Link } = ReactRouterDOM;

export function About() {

    return (
        <section className="about-page">
            <h1>About page - always on display</h1>
                   </section>

    );
}

